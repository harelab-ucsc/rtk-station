network:
  version: 2
  renderer: networkd

  ethernets:
    end0:
      dhcp4: true   # connects to lab/field network, gets IP via DHCP

  wifis:
    wlan0:
      dhcp4: false
      addresses:
        - 172.31.106.2/24
      # hostapd manages the AP association; netplan just assigns the static IP
